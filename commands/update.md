---
description: Longitudinal follow-up - fold new results into an existing case, compute interval changes, and re-convene the board in delta mode
argument-hint: "<case-id> <path to new documents>"
---

Load and follow the plugin's `case-update` skill for: **$ARGUMENTS**
