---
description: Build the deterministic 3D scene from a case's DICOM disc (TotalSegmentator -> browser viewer). No AI interprets pixels; communication aid only
argument-hint: "<case-id> [--fast] [--mr]"
---

Load and follow the plugin's `imaging-3d` skill for: **$ARGUMENTS**

If prerequisites are missing, run `scripts/setup_imaging.sh` first (warn about download size and segmentation runtime before starting).
